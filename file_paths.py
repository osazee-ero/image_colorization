images_folder = r'training_data/training_images'
valid_images_folder = r'training_data/testing_images'

training_annotations = r'training_data/training_annotations'
testing_annotations = r'training_data/testing_annotations'

checkpoint_path = r'checkpoints/'
# must have / at the end

full_imagecolorization_weights = r"YOUR PATH"
instance_imagecolorization_weights = r"YOUR PATH"

recent_weights = r"YOUR PATH TO Recent weights"

output_path = r'output_images/'
# must have / at the end
